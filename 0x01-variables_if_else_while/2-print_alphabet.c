#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Description: '2-print_alphabet'
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	char al = 'a';

	for (al = 'a'; al <= 'z'; al++)
	putchar(al);
	putchar('\n');
	/* your code goes there */
	return (0);
}
